import assert from 'node:assert/strict';import {DatabaseSync} from 'node:sqlite';import {initKKStore,kkPut} from './kk-store.mjs';import {adminJourney} from './kk-admin-journey.mjs';
await initKKStore({sqlite:new DatabaseSync(':memory:')});await kkPut('participant','u','u',{fullName:'Test',station:'A'});await kkPut('submission','a','u',{kind:'proof',station:'A',journeyAt:200});await kkPut('submission','b','u',{kind:'proof',station:'B',journeyAt:100});await kkPut('submission','private','other',{kind:'proof',station:'Secret'});await kkPut('hilta-current','b','u',{km:20,status:'admin-confirmed'});await kkPut('claim','u','u',{km:25,attachments:{}});const d=await adminJourney('u');assert.equal(d.proofs.length,2);assert.equal(d.proofs[0].id,'b');assert.equal(d.proofs[0].previousProofId,'a');assert.equal(d.proofs[0].route.km,20);assert.equal(d.claim.km,25);await assert.rejects(()=>adminJourney('missing'));console.log('PASS journey ownership, chronology, route and claim');
