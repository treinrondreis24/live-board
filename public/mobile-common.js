function esc(v){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}
function marker(t){return (t.eventMode||t.mode)==="arrival"?"ᵃ":"ᵛ"}
function timeHtml(t){const p=t.plannedTime||t.time||"--:--",c=t.currentTime||t.time||p;if(c!==p&&c!=="--:--")return `<span class="m-old">${esc(p)}</span>${esc(c)} <small>${marker(t)}</small>`;return `${esc(p)} <small>${marker(t)}</small>`}
function card(t,{italy=false}={}){const statusClass=(t.type||"").replaceAll(" ","-");return `<article class="m-card ${t.cancelled?"cancelled":""} ${italy?"it-card":""}"><div class="m-top"><div class="m-time">${timeHtml(t)}</div><div class="m-train">${esc(t.train||[t.category,t.number].filter(Boolean).join(" "))}</div><div class="m-track">${esc(t.track&&t.track!=="—"?`sp ${t.track}`:"")}</div></div><div class="m-route">${esc(t.from||"—")} → ${esc(t.to||"—")}</div><div class="m-meta"><span class="m-scan">${esc(t.observedAt||t.station||"")}</span><span class="m-status ${statusClass}">${esc(t.status||"")}${t.trend30==="up"?" ↑":t.trend30==="down"?" ↓":""}</span></div></article>`}
function renderList(el,items,opts){el.innerHTML=items?.length?items.map(x=>card(x,opts)).join(""):`<div class="m-empty">Geen actuele treinen gevonden.</div>`}
function fmtUpdated(v){if(!v)return "—";const d=new Date(v);return new Intl.DateTimeFormat("nl-NL",{hour:"2-digit",minute:"2-digit",second:"2-digit"}).format(d)}
