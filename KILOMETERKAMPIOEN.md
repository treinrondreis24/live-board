# Kilometer Kampioen — implementation status

Branch starts from production main 3f9b660204b6679d6cb8be516d81f305113616ed.

Implemented first phase: `/kilometerkampioen/` email-code authentication, editable participation profile, `/treinhuis` owner-authenticated participant list and readiness display. Uses shared PostgreSQL/SQLite connection and separate `kk_records` table, outside train retention. No participant data returned by anonymous session requests. OTPs expire in 10 minutes and are consumed atomically on first verification attempt; a wrong attempt requires a new code. Per-address and HMAC-IP request limits. Seven-day server sessions with Secure/HttpOnly cookies. No localStorage of participant data. Database migration only creates tables/indexes.

Required before production login: `KK_AUTH_SECRET` (stable random secret; owner encryption/password secret used as fallback), `RESEND_API_KEY`, `KK_EMAIL_FROM` (verified sender). Never put these in Git. Resend requests use the official HTTPS email endpoint and a timeout. No real mail sent during unit tests.

Memory: DB hour cache capped at 4000 entries/36h; station resolution at 500/7d; Italian journey lookup at 500/24h. Periodic cleanup runs independently of traffic. Archived observations are unaffected. RAM reduction must still be measured in production; other memory improvements need profiling.

NOT YET IMPLEMENTED: upload storage, proof/update forms in production, public liveblog and rich editor, citations, team management, map/planning integration, station integration, multiple admin roles. Existing prototype is design reference, not implementation. Do not announce competition readiness or publish a finished app based on this phase.

Next: resolve mail and object-storage provisioning with user, then implement private uploads with server validation and ownership checks (4 images <=15MB each + 1 video <=100MB/60s). Keep proof originals; separate public editorial media and private evidence. All eight participation fields remain: email/full name/display name/edition/start time required; companion conditional; station/distance optional pending user's field-number clarification. No score calculations in v1.

Tests: `node test-kk.mjs`, existing admin/CMS regressions, syntax check. Production deployment and real email verification still required. PostgreSQL integration tests and real-browser checks still required.


2026-09-13 follow-up (local, not deployed): proof and update forms added, authenticated raw uploads streamed to a temporary file with 2 concurrent uploads per process and per-user hourly limits. ffprobe inspects file format/duration before private S3 storage; max 4 photos/1 video per submission. User controls remain intact after errors while the page stays open, with completed upload reuse and idempotent submission IDs. No persistent browser draft across reloads. Owner-only history and media links; Treinhuis owner can list/search submissions and open coordinates on OpenStreetMap. Original uploads retained. No image resizing yet. HEIC/HEIF support depends on bundled ffprobe and needs real-device verification; actual S3/video/browser end-to-end tests still outstanding. Public participant feed, rich editorial editor/quotes and liveblog not implemented. Earlier NOT YET list describes the first phase only; these local changes supersede its forms/storage-handler items.
Validation: authentication/CMS/stations regression suites passed, submission privacy and deduplication passed, installed probe tested with real PNG, syntax and diff checks passed. No provider credentials configured, no real mail sent, no production deployment. GitHub push previously blocked on authentication.


Liveblog follow-up: local editorial draft/published records, explicit publish/withdraw, text-only participant update citation snapshot (no private evidence, coordinates, email or participant photos). Editor supports bold/italic/subhead/list/HTTPS links via simple markup and safe DOM preview. Up to four separately uploaded editorial photos. Public /kilometerkampioen/liveblog iframe allows HTTPS embedding; all admin/participant frames remain denied. Public media route verifies a published post before granting a 120-second storage URL. Already granted URLs may remain usable until expiry after withdrawal. Public updates poll every 30 seconds when visible; no server-side blog cache yet. Editor intentionally uses one shared owner account; concurrent edit conflict handling and draft-loss warning when switching pages are not yet implemented. Latest 200 entries displayed. Browser visual verification, real storage/provider checks and deployment still outstanding. Regression tests include anonymous publishing/upload denial, draft isolation, quote privacy and withdrawal.

Deployment check 2026-09-13: GitHub branch push succeeded. Resend UI still Pending; public DNS NS records are zara.ns.cloudflare.com and kai.ns.cloudflare.com. Required resend._domainkey TXT and send MX are not present at public resolver. User entered records at registrar TransIP, but active zone is Cloudflare; correct there without changing nameservers. Railway still has 3 staged changes including unrelated preexisting PKP key; no deployment applied. Isolated browser smoke test of actual Treinhuis assets passed: admin sections load and markup produces preview heading/text. Mock API only; not end-to-end provider verification. No real email sent.

Community follow-up: authenticated /api/updates uses SQL keyset pagination (30 posts) and multi-team filtering, explicit participant-safe fields and update-only media access. Admin team code/name assignment groups accounts; unassigned participants use their display name as solo groups. Team catalog uses existing 200-record list bound (event has 180 participants). Reizen reuses /app/#tijden in same-origin iframe, preserving existing same-browser station preferences. Live map/planning still awaiting partner URLs. No persistent draft improvement in this step. Tests: pagination with equal timestamps and multiple owners, team grouping, private field/proof exclusion, anonymous route denial and existing suites passed. Real-device UI and provider tests still pending; not deployed.

Manual registration: password signup/login now available without Resend. Passwords use salted async scrypt in separate private records, bounded concurrent derivations, IP/email hourly throttles and existing 7-day HttpOnly sessions. New accounts are pending, may view/share updates, cannot submit/read private proof or owner media links until owner approves in Treinhuis. Approval can be revoked; participant is reloaded per request. Duplicate registration never replaces an existing participant/password. Existing email-verified accounts retain prior rights. No self-service password reset yet; no reset bypass. Tests cover duplicate attempts, bad password, pending update submission, proof denial, approval/revocation and anonymous admin denial. Not yet production deployed; object storage still required for uploads.

LIVE 2026-09-13: production main advanced to 1f8e2c8, Railway deployment afd0dfad-558d-4712-b5e0-2cd667e86b28 successful. Bucket created and five KK_S3 reference variables deployed; preexisting PKP change applied with explicit user approval. Actual production-container storage test wrote/read a small PNG byte-for-byte (PASS); full upload() pipeline with ffprobe and temporary in-memory test metadata also PASS. Two tiny test objects remain under synthetic owner f repeated 64, not referenced by any participant/public post. Production login page loads. Owner browser session expired, so interactive authenticated browser upload not tested; no real video test yet. Resend/Cloudflare remain deferred. No secrets copied to Git.

## 2026-09-13 participant forms and upload diagnostics
- Existing start time is preserved server-side on participant saves; authenticated admin can edit via /treinhuis/api/start-time.
- Profile requires expected distance and Rotterdam checkpoint time; existing records retained.
- Proof form suggests Dutch stations from shared catalog, allows free location, stores station ID/name and server receipt time separately from GPS time.
- Thank-you screen and filtered own submission links after saving.
- Sharp validates actual JPEG/PNG/WebP/AVIF content, originals retained, 60 MP limit, cache disabled, concurrency one. HEIC support depends on decoder; failures give actionable message and correlation code. Videos remain MP4/MOV/WebM 100 MB/60 seconds.
- Signal-specific failure not reproduced without original file. Admin tables/filters/bulk approval remain next step.

## 2026-09-13 Treinhuis tables
Separate participants/proof/updates/blog/teams/settings panels. Admin submission queries filter in database, 50 rows/page with total, date/edition/owner/text/station/team filters. Details in dialog preserve list state; quote opens liveblog draft. Participant sorting and selected approval batches, including revocation for legacy verified accounts. Targeted JSON patches preserve approval when profile data is saved. Tested 3600 SQLite records plus browser details/quote flow.

## 2026-09-13 Hilta route proposals
132 scorecard edges extracted verbatim from scorekaart2026-aangepast.xlsx with source SHA256 and row IDs (hilta-network.json). Deterministic shortest paths in 0.1 km units; ordered via-stations permit return trips. No timetable/actual-route inference or final score calculation. Only scorecard endpoint stations supported; unknown/intermediate stations remain unresolved rather than guessed. Evidence is saved first; proposal and explicit participant confirmation are separate private records. Previous proof chosen by server receipt order; user explicitly reviews that order. First proof uses start station, editable for proposal. Immutable confirmation records retained with source hash, route and notice. Own-proof history can reopen Hilta; admin proof detail shows proposal/confirmed status. Excel export, score caps/checkpoint accounting and previously discussed photo/feed UX remain subsequent work.
Tests: route 249.3 km Rotterdam-Utrecht-Amersfoort-Zwolle-Groningen; reverse/via/return trips; ownership, pending access, no unchecked confirmation, stale proposal, idempotency; browser review/confirmation succeeded.

## 2026-09-13 update experience and editorial scoreboard
Optional update title, compact timeline cards, inline photo/video, name/participant filters, 30-second exact-new-count polling on visible updates page, separate My updates/My proof links and per-user local text/title drafts. File selection must be repeated after reload; previews removable before upload. Originals retained. New photos get a 1280px JPEG q78 preview; existing photos lazily generate previews (bounded two jobs; fallback original on busy/failure). Quoted text editable; selected source update photos validated against source ownership, proof cannot be quoted. Public blog headers compact; quote byline name only.
Admin scoreboard aggregates current participant-confirmed Hilta distances per proof, counts unconfirmed legs, last received proof and elapsed Amsterdam time from full start date/time. Does not apply scorecard limits or constitute official score. Missing legacy start date explicitly assumed 2026-09-19. Date editable in admin, initial date entered in profile and locked after saving. Test 19 Sept 03:59 to 20 Sept 02:59 = 1380 minutes. Tests cover partial quotes/photo ownership/new count; browser title/cards/draft restore/quote/scoreboard. Signal photo preview 92114 bytes vs original584592 bytes.
